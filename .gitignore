# Python
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
.ruff_cache/
.mypy_cache/

# uv / virtualenvs
.venv/
venv/

# OS
.DS_Store

# Editor
.vscode/
.idea/
*.swp

# Claude Code project-local state (per-project tool permissions,
# agent worktrees, scheduler lock). Global state lives in ~/.claude/.
.claude/

# Build artifacts
dist/
build/

# Local scratch
*.log
.env
.immy/

# Personal notes, scratch scripts, TODO drafts. Not for publication.
raw/

# mlx-clip caches HF model weights under CWD. Keep these out of git —
# a single checkpoint is ~600 MB.
mlx-community/
