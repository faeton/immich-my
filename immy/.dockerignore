# Allowlist: send ONLY what the build needs to the docker daemon. Anything not
# explicitly unignored below (config.yml with secrets, .env, scratch, logs, the
# 578M mlx-community/ weights, tests, deploy) stays out of the build context.
**
!pyproject.toml
!src
!src/**
